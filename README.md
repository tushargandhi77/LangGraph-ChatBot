# LangGraph-ChatBot
A modular, graph-based chatbot built using LangGraph, demonstrating controlled reasoning, multi-step workflows, and tool-calling through stateful graph nodes. This project shows how to structure AI conversations using deterministic edges, memory management, and custom logic to create reliable, debuggable conversational agents.
